# Titan Desktop Branding Assets

Place production icon files here for installer/signing builds:

- `icon.ico` (Windows installer and app icon)
- `icon.icns` (macOS app icon)
- `icon.png` (Linux icon and fallback)

The build config already points to these filenames.
